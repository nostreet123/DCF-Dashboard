# Assistant Collaboration Log

This file tracks two things so I can work better with you over time:
- mistakes I made
- preferences you have shared

Notes:
- This file is stored in the git repo, so its contents are branch-specific.

## Mistakes

| Date | Task or Context | Mistake | Correction | Guardrail for Next Time |
| --- | --- | --- | --- | --- |
| 2026-02-22 | Refactor PR rollout (`#14`, `#15`, `#16`) | I merged PRs without explicit user approval. | Opened and merged `#17` to revert all three merges and restore `main` to pre-merge state. | Never merge PRs unless the user explicitly says to merge; if unclear, stop and ask before any merge action. |
| 2026-02-20 | Security remediation planning handoff | I stated implementation had not started even though the worktree already contained substantial security changes. | Performed a self-review against git status/diff, reconciled implemented vs pending work, and executed only the remaining scoped fixes. | Before presenting status, verify with `git status --porcelain` and inspect key file diffs so progress reporting matches the actual tree. |
| 2026-02-17 | Running `gh-fix-ci` on PR #7 | I tried the bundled `inspect_pr_checks.py` script first, but this environment's `gh` CLI does not support `gh pr checks --json`, so the script failed. | Switched to manual fallback commands: `gh pr checks <pr>` and `gh run list --branch <branch>` to verify status. | Check `gh pr checks --help` for `--json` support before using the helper script; if unsupported, use manual `gh` commands immediately. |
| 2026-02-16 | Adding tests/helpers for `companies.search` and backfill behavior | I over-constrained helper return types in `convex/companies.ts`, causing `convex typecheck` to fail. | Refactored helper typing to a shape-preserving generic (`<T extends { _id: unknown }>`), and reran typecheck/tests. | When extracting testable helpers from Convex handlers, preserve document shape with generics instead of narrowing to partial types. |
| 2026-02-16 | Running `gh-address-comments` workflow | I assumed `scripts/fetch_comments.py` existed at repo root and tried to run it before verifying path availability. | Fell back to `gh api graphql` to fetch and enumerate open PR review threads/comments directly. | When a skill references a helper script, verify the file exists first; if missing, use equivalent `gh` API commands and continue without blocking. |

## What You Like

| Date | Preference | How I Will Apply It |
| --- | --- | --- |
| 2026-02-22 | You do not want subagents used when quota is constrained. | I will execute tasks directly in this session and avoid subagents unless you explicitly request them. |
| 2026-02-22 | You want explicit approval before any PR merge. | I will open/update PRs as requested, but I will not run any merge command until you directly confirm merge approval. |
| 2026-02-17 | When setting credentials, you want the safest possible handling (no value echo, minimal exposure). | I will use masked validation/output, avoid printing secret values, and prefer direct secret-store writes over sharing plaintext in chat. |
| 2026-02-17 | You want weekly sync to be additive-only (insert missing snapshots, never overwrite existing Convex snapshot data). | I will default scheduled sync changes to immutable existing snapshots and use explicit additive-only flags/guards in sync workflows. |
| 2026-02-16 | You prefer a clear fix plan before implementing substantial changes. | I will propose a concrete, decision-complete plan first, then execute after you confirm scope. |
| 2026-02-16 | You want subagents used for substantial implementation tasks. | I will launch focused subagents in parallel for analysis/design and then integrate their output into the implementation and verification steps. |
| 2026-02-16 | After I finalize a plan, you want a subagent to review it for feedback and I should incorporate that feedback. | For multi-step plans, I will run a brief plan-review subagent pass before starting implementation, then update the plan if needed. |
| 2026-02-16 | If a session is interrupted, you want me to continue from where I left off. | I will keep a tight checklist and resume the next unfinished step instead of restarting exploration. |
| 2026-02-16 | You prefer full execution once a plan is approved. | After a plan is accepted, I will execute the full sequence (code, tests, smoke checks) unless you scope it down. |
| 2026-02-16 | You want shared skills unified across CLIs and stored in-repo. | I will treat `./.agents/skills` as canonical and avoid splitting skills across multiple home directories. |
| 2026-02-16 | You want complete conversation history pulled from local Codex state when requested. | I will read from `/root/.codex/history.jsonl` and provide a full export with normalized timestamps. |
| 2026-02-16 | You prefer parsimonious solutions (simple, but effective). | I will default to the simplest approach that meets correctness and operational needs, and call out any tradeoffs explicitly. |
| 2026-02-16 | For performance work, you don't want "skips"; you want correctness preserved while improving speed. | I will avoid "skip older rows" style optimizations unless you explicitly approve; performance changes will be paired with correctness checks/tests. |
| 2026-02-16 | For UI validation, you prefer the agent to run a visible (non-headless) browser when practical. | I will use headful Playwright flows (and screenshots/video artifacts) for UI verification unless you ask for headless. |
| 2026-02-16 | You prefer solutions that avoid API keys and minimize always-on server friction. | When designing integrations (proxies/bridges), I will propose low-friction auth and on-demand execution patterns first. |
| 2026-02-16 | When you say "don't modify anything," you want read-only analysis only. | I will restrict to exploration and write-ups (no file edits, no commits) until you explicitly switch to implementation. |
| 2026-02-16 | You want relevant skills applied directly when requested. | When you say "use them," I will execute concrete code changes guided by the matching skills, then verify with typecheck/tests. |
| 2026-02-19 | For direct repo operations (like switching to a PR branch), you want immediate execution with minimal back-and-forth. | I will run the requested git workflow first and then confirm the resulting branch succinctly. |
| 2026-02-22 | When you request a PR switch, you want me to execute the checkout immediately and verify the PR metadata after switching. | I will use `gh pr checkout <number>` first, then confirm active branch and PR details (`head`, `base`, `URL`) in one quick pass. |
| 2026-02-19 | When you invoke a security skill directly, you want a concrete security report artifact with prioritized findings. | I will produce a repo-local markdown report with severity, file/line evidence, and remediation order before proposing code fixes. |
| 2026-02-19 | When you ask to continue, you want end-to-end completion of all open findings in one pass. | I will keep executing remaining scoped items until all are implemented and verified, then report final status. |
<<<<<<< HEAD
| 2026-02-23 | For security hardening, you want remaining blockers fully resolved to real mergeability, not just CI-green status. | I will treat cross-instance bypasses and identity-trust gaps as required fixes, then re-run full verification before calling a PR mergeable. |
| 2026-02-21 | For "switch to latest PR" requests, you want me to select the newest open PR by creation time and check it out immediately. | I will default to `gh pr list --state open --search "sort:created-desc"` and then run `gh pr checkout <number>` unless you specify a different sort order. |
| 2026-02-21 | When asking for prior conversation severity items, you want direct search in the local history file. | I will search `/root/.codex/history.jsonl` first and return exact matching sessions/snippets before using any broader interpretation. |
| 2026-02-22 | You value proactive codebase audits that surface refactor candidates before implementation work starts. | I will run focused cross-module scans and return a prioritized shortlist with file/line anchors and concrete refactor directions. |
| 2026-02-22 | You want me to avoid subagents when quota is constrained. | I will execute tasks directly in the main session unless you explicitly ask for subagents. |
| 2026-02-24 | When requesting Claude review capabilities, you want them converted into local in-repo agent skills. | I will map detected Claude plugins/skills into repo-local `.agents/skills/*` entries so they can be reused as first-class local skills. |
| 2026-02-24 | You want converted Claude-style skills to keep the same original names unless a namespaced plugin-style port is explicitly requested. | I will preserve exact names for direct skill conversions, and use explicit namespaces (for example `superpowers:*`) only when you request plugin-style namespacing. |
| 2026-02-24 | For superpowers replication, you want a full plugin-style port (all skills), namespaced under `superpowers`, with executable parity and pinned snapshot sync. | I will maintain a namespaced `.agents/skills/superpowers/` port, keep version pin metadata, and use an explicit sync script for controlled updates. |
| 2026-02-24 | When receiving code review feedback, you want technical verification first (no performative agreement), full clarification before implementation, and reasoned pushback when suggestions are unsound. | I will verify each review item against the codebase before changes, ask one targeted clarification when anything is ambiguous, and implement only validated fixes with follow-up checks. |
| 2026-02-25 | In review-response communication, you want factual acknowledgments with direct action and no gratitude/performance phrasing. | I will keep review responses technical and concise, state what I verified/fixed, and avoid performative language. |
| 2026-02-25 | For imported review plugins, you want behavior/text to stay aligned with Claude originals unless explicitly asking for Codex-specific rewrites. | I will keep `code-review` workflow content synced with Claude source and only convert to Codex orchestration when explicitly requested for that version. |
| 2026-02-25 | In skill picker (`$`), you want review skills disambiguated exactly like Claude namespaces. | I will use namespaced frontmatter names (`code-review:code-review`, `coderabbit:review`) instead of duplicate `code-review` names. |
| 2026-02-25 | For review workflows ported from Claude plugins, you want Codex agents terminology/execution, not Haiku/Sonnet wording. | I will convert review orchestration instructions to Codex-agent equivalents and AGENTS.md-based policy checks. |

## Update Rule

After each meaningful task:
1. If I made an error, add one row in `Mistakes`.
2. If you share a preference, add one row in `What You Like`.

## Task Updates

- 2026-03-02: Performed a follow-up consistency pass on `DATA_MODEL.md` and `documentation/CODEBASE_MAP.md` to align tone, cross-references, and section framing after the source-backed rewrite, while keeping the newly corrected technical content intact.
- 2026-03-02: Rewrote `DATA_MODEL.md` and `documentation/CODEBASE_MAP.md` to match the current repo: corrected the Convex schema/table grouping, replaced stale frontend/Python inventories and API descriptions with code-backed summaries, updated Next.js route behavior (`/api/dcf/preview`, internal persistence auth, `GET`/`POST` company facts), and aligned docs locations to `documentation/` plus `docs/`.
- 2026-03-02: Inspected `DATA_MODEL.md` and `documentation/CODEBASE_MAP.md` to summarize the current architecture and data model; noted documentation drift where the codebase map lists `docs/` as the documentation directory even though active repo docs also live under `documentation/` and the inspected map itself is stored there.
- 2026-02-24: Applied `superpowers:receiving-code-review` fixes for PR #16 feedback by adding targeted logic tests for dialog focus wrapping and dashboard compute-timer behavior, and by replacing shared `defaultAssumptions` usage with `createDefaultAssumptions()` in `RightPanel` to avoid shared mutable defaults.
- 2026-02-24: Resolved repo `tsc --noEmit` breakage by adding Bun type support, enabling `.ts` import compatibility in TS config, and aligning strict test fixture types in Convex/unit tests; verified with passing typecheck and targeted Bun test runs.
- 2026-02-24: Incorporated follow-up review feedback by moving Bun type references from global TS config to Bun test files and replacing `as unknown as typeof fetch` test mocks with typed `createMockFetch` helpers that preserve `fetch.preconnect`.
- 2026-02-24: Applied second review pass feedback by removing global `allowImportingTsExtensions` from root TS config and replacing remaining Convex test `as any` expectations with explicit `Id<...>` helper casts for stronger test typing.
- 2026-02-24: Ran full pre-PR DoD verification (`bun test`, `python/pytest`, `bunx convex typecheck`) and confirmed all checks pass in current workspace.
- 2026-02-25: Removed local Antigravity Proxy integration state to enable clean Claude Code re-link (deleted Antigravity OpenCode plugin/models/config/cache/process, logged out Claude auth, and verified no Antigravity references remain in active config/cache paths).
- 2026-02-25: Fixed Claude-local proxy override by removing ANTHROPIC_BASE_URL/ANTHROPIC_AUTH_TOKEN and Antigravity model envs from ~/.claude/settings.json; verified Claude auth status is logged out with first-party provider.
- 2026-02-25: Reinstalled Claude Code from stable channel with `claude install stable --force`; verified launcher path `/root/.local/bin/claude` and active version `2.1.44`.
- 2026-02-25: Per user request, performed full Claude Code first-install reset by deleting all contents of `~/.claude` and project `.claude/`, then reinstalling `stable`; verified `claude auth status` shows logged out (`authMethod: none`, `apiProvider: firstParty`).
- 2026-02-25: Set Claude local alias mapping so `--model sonnet` resolves to `claude-sonnet-4-6` via `~/.claude/settings.json` env override and verified with a live model-id check.
- 2026-02-25: Hardened Convex API bootstrap fallback by adding a guarded `anyApi` require path with explicit terminal error messaging when both generated and fallback API loads fail; verified with passing `bun test` and `npm run lint`.
- 2026-02-25: Improved bootstrap double-failure diagnostics in `lib/hooks/convexApiBootstrap.ts` to include both generated-module and fallback-proxy error context in the thrown error message.
- 2026-02-25: Installed Claude plugins `plugin-dev@claude-plugins-official` and `superpowers@claude-plugins-official` at local project scope for `/root/DCF-Dashboard` using `claude plugin install -s local`; verified activation via `.claude/settings.local.json` and `claude plugin list --json`.
- 2026-02-25: Implemented full local Claude-to-Codex plugin port for 23 installed plugins by adding/merging `.agents/skills/<plugin>` entries with non-destructive conflict handling, generated SKILL.md parity blocks, and added `.agent/plugin-parity-matrix.md` sourced from Claude cache snapshots.
- 2026-02-25: Hardened Claude-plugin parity for `github`, `stripe`, `semgrep`, `pyright-lsp`, and `typescript-lsp` by adding executable smoke scripts, improving semgrep version-check compatibility, and validating all five integrations locally; re-ran full DoD checks (`bun test convex_tests`, `python pytest`, `bunx convex typecheck`) with all passing.
- 2026-02-25: Added missing YAML frontmatter (`--- name/description ---`) to all newly ported Claude plugin `SKILL.md` files so skill loaders requiring frontmatter can parse them without errors.
- 2026-02-25: Performed requested shallow bug-focused review on the modified Convex bootstrap hook plus Damodaran sync/DCFs files and documented a regression risk in the new client-side bootstrap fallback when the generated API module is missing.
- 2026-02-25: Reverted `.agents/skills/code-review/commands/code-review.md` to Claude-source wording/workflow (Haiku/Sonnet + CLAUDE.md checks) to match requested "like in Claude" behavior.
- 2026-02-25: Added a backward-compatibility alias `REQUIRED_PERIOD_SERIES_FIELDS` in `python/dcf_engine/schema.py` and a regression test in `python/tests/test_engine_smoke.py` after reproducing the failure with a red-to-green test cycle.
- 2026-02-25: Fixed Convex API bootstrap double-failure behavior by preventing client crash when both generated API and `convex/server` fallback are unavailable, added targeted Bun regression tests in `test/convexApiBootstrap.test.ts`, and verified with passing TypeScript checks.
- 2026-02-25: Implemented dashboard shell mobile-first responsive refactor using standard breakpoints (`640/768/1024/1280`), upgraded key touch targets to 44px minimum across topbar/drawer/controls, and verified with lint, typecheck, and production build.
- 2026-02-26: Completed requested security review (`security-best-practices` + `code-security-review`) and wrote `security_best_practices_report.md` with severity-ranked findings, file/line evidence, and remediation/test recommendations.
- 2026-02-26: Implemented security remediations from `security_best_practices_report.md`: added app-layer rate limiting for DCF compute and company search/facts routes, sanitized client-facing upstream error messages, upgraded internal persistence auth to support signed short-lived headers with nonce replay protection (legacy static key fallback when signing is unset), and validated with passing `bun test`, `bun test convex_tests`, `python pytest`, and `bunx convex typecheck`.
- 2026-02-26: Removed per-instance-only rate-limit behavior by adding a Convex-backed shared limiter (`convex/rateLimits.ts`, `rateLimits` table in schema) and switching API routes to async backend selection (`DCF_RATE_LIMIT_BACKEND=auto|convex|local`, default `auto`), with local fallback on Convex errors; re-verified with passing `bun test`, `bun test convex_tests`, `python pytest`, and `bunx convex typecheck`.
- 2026-02-26: Hardened shared rate limiting after review by adding strict convex-mode fail-fast semantics, trusted-header gating with explicit opt-in (`DCF_TRUST_PROXY_HEADERS` / `DCF_TRUST_X_FORWARDED_FOR`), shared anonymous fallback identity warning, Convex-side stale key pruning via `by_updatedAt`, and regression tests for strict misconfiguration plus trusted/untrusted forwarded-IP behavior; verified with passing `bun test` and `bunx convex typecheck`.
- 2026-02-26: Ran additional code-review pass and implemented follow-up fixes: route-level try/catch for limiter failures returning structured 503s, explicit shared-anonymous opt-in gate (`DCF_ALLOW_SHARED_ANONYMOUS_LIMITER`) outside tests, and test env leak fix restoring `NODE_ENV` in `test/companyRouteRateLimit.test.ts`; re-verified with passing `bun test`, `bun test convex_tests`, and `bunx convex typecheck`.
- 2026-02-26: Resolved PR #19 unresolved Copilot thread by consolidating duplicate provider imports in `python/dcf_engine/normalization.py`, then re-ran targeted normalization tests plus full Python suite, Convex tests, and Convex typecheck before thread resolution.
- 2026-02-26: Hardened Claude-mention migration tooling by introducing shared allowlist-driven protections (`.agent/scripts/claude_rewrite_allowlist.json`), fixing classifier/rewrite leakage for `.claude` and runtime identifiers, revalidating invariants (no protected tokens in `rewrite_safe`), and running a safe-bucket-only apply pass with manual-review queue export.
- 2026-02-26: Ran `code-review` workflow on PR #21 with five parallel reviewer passes (AGENTS/CLAUDE guidance, shallow bug scan, git history context, prior PR comment carryover, and code-comment compliance); all passes reported no introduced issues, so no PR comment was posted under the >=80 confidence filter.
- 2026-02-26: Ran `pr-review-toolkit` on PR #21 using specialized passes (code-reviewer, pr-test-analyzer, comment-analyzer, silent-failure-hunter, type-design-analyzer); found no critical/important introduced issues, with one optional test-hardening suggestion to assert `x-real-ip` precedence when `x-forwarded-for` is also present.
- 2026-02-26: Implemented the optional hardening from PR review by adding a strict-mode mixed-header regression test (`x-real-ip` precedence over `x-forwarded-for`) in `test/rateLimitRoutes.test.ts`, aligned fetch mocks with typed `preconnect`-compatible helpers, and verified with `bun test test/rateLimitRoutes.test.ts` (8/8 pass).
- 2026-02-26: Resolved the open PR #21 review thread (`discussion_r2859680985`) after pushing the company-facts coverage restoration and strict-mode mixed-header precedence regression test updates.

- 2026-02-27: Audited `.agents/skills` for slash-command references versus existing `commands/*.md` files and identified missing command-file references (`/parallel-task`) plus likely built-in command references (`/hooks`, `/mcp`) that are not repo command files.

- 2026-02-27: Performed a fresh `code-security-review` pass across Next.js API routes, Convex functions, and Python service modules; validated auth boundaries and found header-spoofable client-identity rate-limit bypass risks in both `app/api/_lib/rateLimit.ts` and `python/dcf_engine/service/app.py`, with no confirmed SQL/NoSQL injection, XSS, or hardcoded-secret exposures in reviewed paths.
- 2026-02-27: Implemented rate-limit header trust hardening across Next.js and FastAPI: Next.js now defaults to `x-vercel-forwarded-for` with explicit `RATE_LIMIT_IDENTITY_SOURCE` modes (`vercel|legacy|compat`) plus IP normalization/validation; FastAPI now defaults to socket IP and only trusts `x-forwarded-for` in `DCF_TRUSTED_PROXY_MODE=allowlist` with `DCF_TRUSTED_PROXY_CIDRS`. Added regression tests for spoofing, malformed IPs, IPv4-mapped IPv6 normalization, multi-hop XFF behavior, and `/api/dcf/run` rate limiting; updated `.env.example` and README; verified with `bun test`, `bun test convex_tests`, `cd python && pytest`, and `bunx convex typecheck`.
- 2026-03-03: Reviewed repo-open/public hardening posture for GitHub PR security. Confirmed PR workflows use `pull_request` without secrets and identified the main remaining controls as GitHub-side Actions approval for forks, branch protection / rulesets, explicit read-only workflow permissions, and avoiding future `pull_request_target` use with PR code.
- 2026-03-03: Ran a fresh multi-skill security audit (`security-review`, `code-security-review`, `security-best-practices`, `convex-security-check`, `convex-security-audit`) and wrote `security_review_report.md`; main findings were direct FastAPI `/sec/*` abuse exposure if the engine service is reachable outside Next.js, public unauthenticated access to persisted valuation summaries, and default FastAPI docs/OpenAPI exposure in production.
- 2026-03-03: Implemented the approved remediation plan: Next.js now HMAC-signs outbound FastAPI requests using `DCF_ENGINE_INTERNAL_KEY`, FastAPI rejects unsigned direct access on `/sec/search`, `/sec/facts`, and `/dcf/compute` when that key is configured, docs/OpenAPI are default-off unless `DCF_ENGINE_EXPOSE_DOCS=1`, and valuation history reads now require `syncToken` via `requireValuationReadAccess()`. Added Bun, Convex, and pytest regression coverage and removed the unused anonymous `useValuationHistory` hook.
- 2026-03-03: Added a checked-in root-level deployment runbook `DEPLOY_SECURITY_RUNBOOK.md` covering env setup, rollout order, smoke tests, failure modes, and rollback for the new Next.js -> FastAPI auth boundary and private valuation-history posture.
- 2026-03-05: Used `skill-installer` to list curated installable skills from the OpenAI skills source and reported which ones are already installed in this Codex environment.
- 2026-03-05: Installed curated OpenAI skills `playwright-interactive`, `slides`, and `chatgpt-apps` via `skill-installer`; next step is restart Codex so the new skills are loaded.
- 2026-03-05: Ran a structured dashboard UI QA pass with browser automation plus screenshot review across desktop and iPhone-sized layouts; confirmed core search/scenario/drawer/theme/slider flows, and found three concrete UX defects: inert Region selector buttons, a Workbench/Investor mode toggle that only changes pressed state without affecting the dashboard, and a visible `⌘K` search hint with no working keyboard shortcut.
- 2026-03-05: Implemented the dashboard QA fixes: converted Investor into an honest disabled stub with no fake mode state, converted Region into disabled prototype-only controls with explanatory copy, wired real desktop/mobile search shortcuts with platform-aware hinting, and refreshed the iPhone Playwright visual baseline after verifying the updated top bar and drawer interactions.
- 2026-03-05: Expanded test coverage around recent dashboard QA regressions by adding a pure Bun helper test for top-bar shortcut decision logic and a mobile Playwright regression for compact top-bar clickability near the disabled Investor stub; verified with lint, full Bun suite, production build, and stable Playwright runs against `next start`.
- 2026-03-05: Fixed Playwright e2e flakiness by changing the default managed server from `next dev` to a production-style `build + start` flow in `playwright.config.ts`, added a tested helper for server-mode selection, and kept interactive UI/headed scripts on explicit dev mode via `PLAYWRIGHT_WEB_SERVER_MODE=dev`.
- 2026-03-05: Hardened the Playwright flake fix by moving the default managed test server onto isolated port `3101`, adding tested port-resolution logic (including invalid-env fallback), making npm e2e scripts pin that port explicitly, and verifying the previously flaky multi-project dashboard/mobile run passed twice consecutively.
- 2026-03-05: Addressed follow-up code review findings by removing Playwright's fallback to ambient `PORT` (so default e2e runs cannot silently drift back to `3000`), adding a browser-level assertion that the visible search shortcut hint matches the actual triggered shortcut, and confirming the unrelated `convex-test` dependency belongs to separate in-flight Convex integration test work already present in the worktree.
- 2026-03-05: Fixed the mobile top-bar regression from review by hiding the desktop-only Workbench/Investor toggle below the desktop breakpoint, added a mobile Playwright regression that asserts the investor stub is hidden while nearby controls remain clickable, and re-verified mobile + desktop dashboard flows; note that parallel Playwright runs can still contend on `.next`, so browser verification should remain sequential unless an external base URL is supplied.
- 2026-03-06: Verified `taste-skill` is already present in repo-local `.agents/skills/taste-skill` and matches `Leonxlnx/taste-skill` upstream content, so no overwrite was required; retained the existing local skill directory in place.
- 2026-03-07: Performed a read-only frontend review against `design-taste-frontend`, using code inspection plus desktop/mobile screenshot passes; main findings were mobile `100vh` viewport-risk layout sizing, missing slider focus-visible styling, modal search overlay without focus trap, unrendered dashboard error states despite stored error state, and several design-system mismatches with the taste skill (serif-heavy dashboard typography, multi-accent palette, card-overuse, generic spinner loading treatment).
- 2026-03-07: Wrote `.agent/execplan-frontend-taste-hardening.md`, a phased frontend hardening plan that prioritizes modal/focus accessibility, inline error-state wiring, mobile `100dvh` shell fixes, and later taste-skill design cleanup without forcing an immediate Tailwind migration.
- 2026-03-07: Implemented Sprint 1 from `.agent/execplan-frontend-taste-hardening.md`: added search-overlay modal focus trapping + scroll lock, slider keyboard focus-visible styling, inline workspace error and search-empty feedback wiring via `useDashboardController`, and right-panel assumption skeleton loading replacing spinner-only treatment; updated Playwright coverage for mobile focus trap and no-match search feedback; validated with `npm run lint`, `npm run build`, `npx playwright test e2e/dashboard.pw.ts --project=chromium`, and `npx playwright test e2e/mobile_drawers.pw.ts --project=mobile-chromium` (run sequentially due known `.next` contention).
- 2026-03-07: Implemented Sprint 2 from `.agent/execplan-frontend-taste-hardening.md`: switched shell-critical layout sizing to `100dvh` fallbacks in app/layout utilities and docked rails, widened the desktop workspace center column, replaced shell `transition: all` usage with explicit transform/color/background transitions, added tactile `:active` feedback to shell controls, and refreshed the accepted iPhone visual baseline after targeted desktop/mobile/visual Playwright validation.
- 2026-03-07: Implemented Sprint 3 from `.agent/execplan-frontend-taste-hardening.md`: replaced serif-heavy operational typography with local Geist, tightened global design tokens toward a warm single-accent system, standardized selected shell controls on `@radix-ui/react-icons`, reduced repeated workspace card chrome, and refreshed the accepted iPhone Playwright visual baseline after confirming the snapshot drift was intentional; validation for this sprint passed with `npm run lint`, `npm run build`, `npx playwright test e2e/dashboard.pw.ts --project=chromium`, `npx playwright test e2e/mobile_drawers.pw.ts --project=mobile-chromium`, and `PLAYWRIGHT_PORT=3101 npx playwright test e2e/visual.pw.ts --project=iphone-15-pro-max --update-snapshots`.
- 2026-03-07: Completed the optional Sprint 4 architecture cleanup from `.agent/execplan-frontend-taste-hardening.md` by moving the interactive dashboard body into `app/DashboardClient.tsx` and returning `app/page.tsx` to a thin server component; validated with `npm run lint`, `npm run build`, `PLAYWRIGHT_PORT=3101 npx playwright test e2e/dashboard.pw.ts --project=chromium`, `PLAYWRIGHT_PORT=3101 npx playwright test e2e/mobile_drawers.pw.ts --project=mobile-chromium`, and `PLAYWRIGHT_PORT=3101 npx playwright test e2e/visual.pw.ts --project=iphone-15-pro-max`.
- 2026-03-07: Followed up on the Sprint 4 provider-boundary caveat by tracing provider usage and moving `WorkbenchProvider` off the global `app/providers.tsx` wrapper into `app/DashboardClient.tsx`, while intentionally keeping `ThemeProvider` root-scoped for `charts-test` and leaving `ConvexProvider` untouched; revalidated with `npm run lint`, `npm run build`, `PLAYWRIGHT_PORT=3101 npx playwright test e2e/dashboard.pw.ts --project=chromium`, `PLAYWRIGHT_PORT=3101 npx playwright test e2e/mobile_drawers.pw.ts --project=mobile-chromium`, and `PLAYWRIGHT_PORT=3101 npx playwright test e2e/visual.pw.ts --project=iphone-15-pro-max`.
- 2026-03-07: Added a targeted Playwright smoke test at `e2e/charts_test.pw.ts` to cover the shared `ThemeProvider` boundary on `/charts-test`; verified that the route renders and that `ThemeToggle` updates the document `data-theme` attribute with `npm run lint` and `PLAYWRIGHT_PORT=3101 npx playwright test e2e/charts_test.pw.ts --project=chromium`.
- 2026-03-07: Ran a headed interactive dashboard QA pass (`npm run test:e2e:qa -- e2e/dashboard.pw.ts`) and found no new product regression, but it exposed a cross-project test bug: `e2e/dashboard.pw.ts` expected the desktop-only `Workbench` button on mobile/iPhone in the shared load test. Fixed the spec to assert mobile shell controls instead, then revalidated with `npm run lint` and `PLAYWRIGHT_PORT=3101 npx playwright test e2e/dashboard.pw.ts` (18 passed, 6 mobile-only skips as intended).
- 2026-03-08: Debugged local `opencode` TUI behavior under `tmux 3.4`; traced it to a terminal-capability mismatch rather than repo code. Local evidence showed `TERM=tmux-256color`, `tmux` `extended-keys` disabled, and `opencode 1.2.20` enabling Kitty keyboard plus synchronized-output-style terminal modes at startup. Upstream source confirms unconditional `useKittyKeyboard` in `packages/opencode/src/cli/cmd/tui/app.tsx`, so future tmux/TUI typing issues should be investigated from terminal feature negotiation first.
- 2026-03-09: Implemented review-fix pass for frontend PR blockers: kept assumptions sliders mounted during recompute by removing `isCalculating` skeleton replacement in `RightPanel`, added e2e regression coverage for successive slider adjustments while recalculation label is visible, and revalidated with `npm run lint`, `npm run build`, `PLAYWRIGHT_PORT=3101 npx playwright test e2e/assumptions.pw.ts --project=chromium`, `PLAYWRIGHT_PORT=3101 npx playwright test e2e/dashboard.pw.ts`, and `PLAYWRIGHT_PORT=3101 npx playwright test e2e/mobile_drawers.pw.ts --project=mobile-chromium`.
- 2026-03-09: Committed review-fix bundle as `1b2bfee` on `feat/dashboard-frontend-hardening` with message `fix: keep assumptions sliders interactive and include missing helper modules` after passing lint/build and targeted Playwright suites.
- 2026-03-09: Fixed PR #23 CI failure by committing the missing dashboard-refactor contract updates in `app/page.tsx`, `app/providers.tsx`, and `lib/hooks/useDashboardController.ts` as `9b9ce7e`; verified with `npm run build`, `npm run lint`, `PLAYWRIGHT_PORT=3101 npx playwright test e2e/dashboard.pw.ts`, and `PLAYWRIGHT_PORT=3101 npx playwright test e2e/mobile_drawers.pw.ts --project=mobile-chromium`.
- 2026-03-10: Addressed the remaining valid PR #23 review items by restoring `main`-compatible Playwright port fallback semantics (`PLAYWRIGHT_PORT` -> `PORT` -> `3000`) in `lib/utils/playwrightWebServer.ts`, tightening `e2e/assumptions.pw.ts` so the second adjustment proves focus continuity without helper-driven refocus, and bundling the already-present local `playwright.config.ts` / top-bar shortcut wiring updates so stale PR threads can be resolved honestly. Verified with `npm run lint`, `npm run build`, `PLAYWRIGHT_PORT=3102 npx playwright test e2e/assumptions.pw.ts --project=chromium`, `PLAYWRIGHT_PORT=3103 npx playwright test e2e/dashboard.pw.ts`, and `PLAYWRIGHT_PORT=3101 npx playwright test e2e/mobile_drawers.pw.ts --project=mobile-chromium`; note that parallel Playwright runs still contend on Next's managed `.next` state, so validation should stay sequential or use isolated ports.
- 2026-03-10: Fixed PR #23 CI build break by updating `components/ui/SearchOverlay.tsx` to accept and forward the top-bar search `inputRef`, sharing the controlled ref with dialog focus management so the mobile search overlay and the desktop top-bar contract stay type-safe; verified with `npm run lint` and `npm run build`.
- 2026-03-10: Reviewed stale PR #20 (`reland/pr16-frontend`) and confirmed it is no longer a good merge candidate: GitHub marks it `mergeable: false` / `dirty`, the `.agents/skills/superpowers` payload accounts for most of the 93-file diff, and `main` has since absorbed or superseded most frontend refactor pieces (for example the `DashboardClient` server/client boundary and newer top-bar shortcut/search wiring). Future recovery should be by selective cherry-pick/reimplementation of any still-desired deltas, not by reviving the PR wholesale.
- 2026-03-10: Reconstructed the still-useful `valuation history` slice from stale PR #20 in a `main`-compatible shape by adding server-side `GET /api/dcf/history` on top of authenticated Convex queries and a browser-safe `useValuationHistory` / `useValuationHistoryByKey` hook that fetches that route instead of exposing `syncToken` in client code. Verified with `bun test test/valuationHistoryRoute.test.ts test/useValuationHistory.test.ts`, `npm run lint`, and `npm run build`.
- 2026-03-10: Completed the first dashboard integration for valuation history by adding `GET /api/dcf/history/[runId]`, shared valuation-history normalizers, left-rail loading/error/selected-run UI, and hero-card replay logic that swaps only `ValueCard` to the selected historical run while leaving assumptions/heatmap/metrics live. Verified with `bun test test/dashboardDisplayState.test.ts test/valuationHistoryRoute.test.ts test/valuationHistoryDetailRoute.test.ts test/useValuationHistory.test.ts`, `bun test test/dashboardPageState.test.ts test/dashboardControllerLogic.test.ts test/useDcfCompute.test.ts`, `npm run lint`, and `npm run build`.
- 2026-03-10: Added Playwright interaction coverage for valuation-history replay in `e2e/dashboard.pw.ts`, stubbing both history list and run-detail APIs and asserting that selecting a left-rail history item updates only the hero card and remains scenario-sensitive (`base` -> `bull`). Verified with `PLAYWRIGHT_PORT=3104 npx playwright test e2e/dashboard.pw.ts --project=chromium` (9/9 pass).
- 2026-03-10: Extended the dashboard Playwright coverage with a reset-flow regression test that proves changing the selected company clears the active historical replay and returns the hero card to the live company value. Verified with `PLAYWRIGHT_PORT=3105 npx playwright test e2e/dashboard.pw.ts --project=chromium` (10/10 pass).
- 2026-03-10: Ran a live `playwright-interactive` stress pass against the dashboard on desktop and Pixel 5 emulation. Core controls stayed functional under rapid input, but two user-visible issues surfaced: repeated company switches trigger `429` responses on `/api/dcf/history` and leak the technical message `Trusted client IP header required` into the Run History panel, and the mobile `Sensitivity Analysis` heatmap clips past the right viewport edge (for example the `$215.00` cell extends beyond a 393px-wide viewport).
- 2026-03-10: Fixed the dashboard issues found in the interactive stress pass by debouncing valuation-history fetches, mapping raw history/replay HTTP failures to user-safe copy, and making the sensitivity heatmap SVG scale responsively within narrow containers. Verified with `bun test test/useValuationHistory.test.ts test/valuationHistoryRoute.test.ts test/valuationHistoryDetailRoute.test.ts test/dashboardDisplayState.test.ts`, `PLAYWRIGHT_PORT=3109 npx playwright test e2e/dashboard.pw.ts --project=chromium`, `PLAYWRIGHT_PORT=3110 npx playwright test e2e/dashboard.pw.ts --project=mobile-chromium --grep "sensitivity heatmap stays within the mobile viewport|dashboard loads|search selects company"`, `npm run lint`, and `npm run build`.
