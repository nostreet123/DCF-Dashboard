# Assistant Collaboration Log

This file tracks the current high-signal assistant mistakes and preferences.
Older branch-specific history is preserved in `docs/assistant-log-archive.md`.

## Mistakes

| Date | Task or Context | Mistake | Correction | Guardrail for Next Time |
| --- | --- | --- | --- | --- |
| 2026-02-22 | Refactor PR rollout (`#14`, `#15`, `#16`) | I merged PRs without explicit user approval. | Opened and merged `#17` to revert all three merges and restore `main` to pre-merge state. | Never merge PRs unless the user explicitly says to merge; if unclear, stop and ask before any merge action. |
| 2026-02-20 | Security remediation planning handoff | I stated implementation had not started even though the worktree already contained substantial security changes. | Performed a self-review against git status/diff, reconciled implemented vs pending work, and executed only the remaining scoped fixes. | Before presenting status, verify with `git status --porcelain` and inspect key file diffs so progress reporting matches the actual tree. |
| 2026-02-17 | Running `gh-fix-ci` on PR #7 | I tried the bundled `inspect_pr_checks.py` script first, but this environment's `gh` CLI does not support `gh pr checks --json`, so the script failed. | Switched to manual fallback commands: `gh pr checks <pr>` and `gh run list --branch <branch>` to verify status. | Check `gh pr checks --help` for `--json` support before using the helper script; if unsupported, use manual `gh` commands immediately. |
| 2026-02-16 | Adding tests/helpers for `companies.search` and backfill behavior | I over-constrained helper return types in `convex/companies.ts`, causing `convex typecheck` to fail. | Refactored helper typing to a shape-preserving generic (`<T extends { _id: unknown }>`), and reran typecheck/tests. | When extracting testable helpers from Convex handlers, preserve document shape with generics instead of narrowing to partial types. |
| 2026-02-16 | Running `gh-address-comments` workflow | I assumed `scripts/fetch_comments.py` existed at repo root and tried to run it before verifying path availability. | Fell back to `gh api graphql` to fetch and enumerate open PR review threads/comments directly. | When a skill references a helper script, verify the file exists first; if missing, use equivalent `gh` API commands and continue without blocking. |
| 2026-05-04 | Mac-prototype-to-web parity browser validation | I validated the dashboard while it was still in mock-backed demo mode, which missed the user's requirement that the web path match the Mac app's EDGAR-backed behavior. | Switched the default dashboard data mode to live, made mock data explicit-only, and revalidated through Browser Use plus FastAPI logs showing EDGAR facts and DCF compute. | For parity validation, check the visible data mode and provenance/source before calling the UI validated. |
| 2026-05-04 | BRK-A EDGAR valuation | I routed BRK-A to import when SEC companyfacts lacked a recent direct shares fact, even though official SEC filings could still unlock valuation without manual import. | Added an EDGAR 10-K cover-page shares fallback for Berkshire, deriving Class A-equivalent and Class B-equivalent shares from official filing HTML. | Before declaring import required, check whether another official filing endpoint can provide the missing required field. |

## What You Like

| Date | Preference | How I Will Apply It |
| --- | --- | --- |
| 2026-02-22 | You do not want subagents used when quota is constrained. | I will execute tasks directly in this session and avoid subagents unless you explicitly request them. |
| 2026-02-22 | You want explicit approval before any PR merge. | I will open/update PRs as requested, but I will not run any merge command until you directly confirm merge approval. |
| 2026-02-17 | When setting credentials, you want the safest possible handling. | I will use masked validation/output, avoid printing secret values, and prefer direct secret-store writes over sharing plaintext in chat. |
| 2026-02-17 | You want weekly sync to be additive-only. | I will default scheduled sync changes to immutable existing snapshots and use explicit additive-only flags/guards in sync workflows. |
| 2026-02-16 | You prefer a clear fix plan before implementing substantial changes. | I will propose a concrete, decision-complete plan first, then execute after you confirm scope. |
| 2026-02-16 | You want subagents used for substantial implementation tasks. | I will launch focused subagents in parallel only when you explicitly request subagent delegation. |
| 2026-02-16 | After I finalize a plan, you want a subagent to review it for feedback. | For multi-step plans, I will run that review pass when you ask for subagents or when the current session policy allows it. |
| 2026-02-16 | If a session is interrupted, you want me to continue from where I left off. | I will keep a tight checklist and resume the next unfinished step instead of restarting exploration. |
| 2026-02-16 | You prefer full execution once a plan is approved. | After a plan is accepted, I will execute the full sequence unless you scope it down. |
| 2026-02-16 | You want shared skills unified across CLIs and stored in-repo. | I will treat `./.agents/skills` as canonical and avoid splitting skills across multiple home directories. |
| 2026-02-16 | You prefer parsimonious solutions. | I will default to the simplest approach that meets correctness and operational needs, and call out tradeoffs explicitly. |
| 2026-02-16 | For performance work, you do not want correctness-reducing skips. | I will avoid skip-style optimizations unless you explicitly approve them. |
| 2026-02-16 | For UI validation, you prefer a visible browser when practical. | I will use headful browser flows for UI verification unless you ask for headless. |
| 2026-02-16 | When you say "don't modify anything," you want read-only analysis only. | I will restrict to exploration and write-ups until you explicitly switch to implementation. |
| 2026-02-19 | For security skill requests, you want a concrete report artifact. | I will produce a repo-local report with severity, file/line evidence, and remediation order before proposing fixes. |
| 2026-02-25 | In review-response communication, you want factual acknowledgments with direct action. | I will keep review responses technical and concise, avoiding performative language. |
| 2026-04-28 | For repo validation, you want an agent-legible harness rather than scattered commands. | I will use `npm run harness:verify` as the canonical full gate and `npm run harness:e2e:smoke` for browser-facing changes. |
| 2026-05-02 | You want the repo made more token efficient for agents. | I will keep root agent guidance short, route agents through `AGENT_INDEX.md`, and archive old collaboration notes instead of growing the active log indefinitely. |

## Update Rule

After each meaningful task:

1. If I made an error, add one row in `Mistakes`.
2. If you share a preference, add one row in `What You Like`.
3. Keep this file current and compact; move older detail to
   `docs/assistant-log-archive.md`.

## Recent Task Updates

- 2026-04-15: Exposed the repo skill library globally for Codex by adding missing per-skill symlinks from `/Users/danguncea/Codex/DCF-Dashboard/.agents/skills` into `/Users/danguncea/.codex/skills`, while preserving existing global entries. Verified with the skills inventory script and symlink spot-checks.
- 2026-04-25: Fixed defensive dashboard slop from review: removed the fake timed compute helper, wired dashboard valuation display to real company facts plus `/api/dcf/preview`, switched search to `/api/company/search`, made empty DCF engine 2xx responses fail, and made `/api/dcf/run` persist-or-fail instead of returning success when persistence is unavailable. Verified with `npm run build`; targeted Bun tests could not run because `bun` was not installed in that shell.
- 2026-04-28: Implemented the agent-first harness repair: added repo-local Bun bootstrap, canonical `npm run harness:verify`, targeted `npm run harness:e2e:smoke`, repository invariant checks, CI wiring, docs updates, `.DS_Store` hygiene, and lint scoping for ignored worktrees. Verified with `npm run smoke:alive`, `npm run harness:verify`, and `npm run harness:e2e:smoke`.
- 2026-04-28: Set up the repo for OpenAI Symphony by adding a repo-specific `WORKFLOW.md`, tracking and lightly tailoring upstream-recommended helper skills under `.codex/skills`, and documenting local Symphony startup in `docs/symphony.md`.
- 2026-05-02: Made the repo more token efficient for future agents by adding `AGENT_INDEX.md`, adding `docs/agent-context-policy.md`, adding focused validation script aliases, teaching `AGENTS.md` the context budget rules, and moving the full historical collaboration log to `docs/assistant-log-archive.md`.
- 2026-05-02: Restored dashboard demo/live data integrity: defaulted the UI to mock-backed demo mode with no dashboard API calls, kept live mode explicit, added opt-in browser-readable valuation history routes, and hid dependent analysis when live valuation data is unavailable. Verified with focused Bun tests, `npm run harness:e2e:smoke`, `npm run harness:verify`, `npx playwright test e2e/dashboard.pw.ts --project=chromium`, and `npm run lint`.
- 2026-05-03: Started Mac-prototype-to-web parity with the rich valuation output slice: wired live DCF preview responses into scenario values, sensitivity offsets, projections, KPI strip, Monte Carlo summary, filing provenance, and statement history panels. Verified with focused Bun tests, API/security route tests, `npm run lint -- --quiet`, `npm run build`, Browser Use validation, and a screen recording at `docs/validation-recordings/rich-valuation-output/rich-valuation-output.webm`.
- 2026-05-04: Continued Mac-prototype-to-web parity as web-native vertical slices: added shared coverage/import contracts, official search/detail adapters, Convex-backed import artifacts/facts, CSV/XLSX/PDF parsing, import approval compute/persistence, AI scenario analysis, settings status, browser recents, currency-aware display, replay detail normalization, and the parity checklist.
- 2026-05-04: Corrected parity validation to use live EDGAR-backed mode by default instead of mock demo data. Verified in Browser Use that the left rail shows no mock datasets, settings report SEC configured and active mode live, and the valuation panel reports EDGAR filing provenance for AAPL; also reran focused UI/rate-limit tests, lint, and `npm run build`.
- 2026-05-04: Fixed the desktop search bar by making submit explicit via Enter, form submit, and an icon button. Verified in Browser Use with live EDGAR-backed searches for MSFT and NVDA, plus focused UI tests, lint, and `npm run build`.
- 2026-05-04: Added the missing desktop search dropdown backed by `/api/company/search`, including result metadata, coverage badges, click selection, proper stacking above valuation cards, and close-on-select behavior. Verified in Browser Use with live EDGAR-backed results and re-ran focused UI tests, lint, and `npm run build`.
- 2026-05-04: Verified the DCF engines with focused Python golden/formula tests, focused API/security route tests, and a live NVDA EDGAR valuation path. The direct `/api/dcf/preview` output matched the browser display after comparing against the app's actual scenario defaults: base fair value `$9.15` and Monte Carlo range `$6.02 - $10.78`.
- 2026-05-04: Investigated BRK-A valuation failure. Fixed SEC annual fact alignment by period end year, merged missing modern cash years from the restricted-cash fallback concept, defaulted optional cash/debt bridge fields to zero in the workbench payload, and made SEC coverage checks mark companies missing revenue or shares as `import_required`. Also refreshed stale local recents through detail before selection so old `valuation_ready` entries cannot force a failed compute.
- 2026-05-04: Removed the BRK-A import detour by deriving Berkshire Class A-equivalent shares from the latest official SEC 10-K cover page and scaling BRK-B to Class B-equivalent shares. Also hardened broader `brk` search so one missing SEC companyfacts endpoint no longer breaks the dropdown, and ranked Berkshire tickers above loose prefix matches. Verified BRK-A search returns `valuation_ready`, browser selection computes directly from EDGAR with fair value `$985,041.19`, and high-priced sensitivity labels render compactly in the grid.
- 2026-05-04: Promoted the hero-card Monte Carlo distribution from a small inline sparkline to a larger right-side chart on desktop, with compact axis labels for high-priced shares and stacked responsive behavior on smaller screens. Verified with focused Bun tests, lint, production build, and browser inspection on BRK-A.
- 2026-05-04: Validated Monte Carlo distribution truthfulness: the valuation path uses live engine Monte Carlo samples, and the chart restores the old smooth area visual while deriving its shape from empirical histogram bins rather than mock data.
- 2026-05-04: Added instant ticker logos to the desktop search dropdown by enriching company search results with ticker-logo URLs, rendering CSP-safe Next image thumbnails with fallback initials, and validating the live AAPL dropdown screenshot.
- 2026-05-04: Refined the ticker-logo search dropdown layout by moving ticker symbols to the right column, enlarging the left logo, and folding coverage status into metadata so company names get more scan space.
- 2026-05-05: Added public-demo AI cost controls with strict admin mode: per-IP and global daily AI caps, cached real AI responses, SHA-256-hashed server-side admin token verification, memory-only browser token entry, and focused security validation that no admin secret/hash is bundled into public client code.
- 2026-05-05: Stress-tested the AI admin-token bypass with Codex Security validation: added direct verifier tests for digest-as-token, digest-of-digest, malformed env config, duplicate/comma headers, malformed token shapes, and oversized headers; added route tests proving invalid tokens do not bypass public caps while valid admin mode bypasses only AI demo caps and still respects payload validation.
- 2026-05-05: Updated AI scenario analysis for DeepSeek-V4-Pro-style max reasoning: strict JSON schema responses, `reasoning_effort=xhigh`, configurable large input/output limits, provider parameters, and docs/env defaults for `deepseek-ai/DeepSeek-V4-Pro`.
- 2026-05-05: Live-tested DeepSeek-V4-Pro through Hugging Face and the app route. Found provider `json_schema` responses can return `content: null` under max reasoning; switched DeepSeek default to `json_object` plus server-side strict validation and raised max output tokens to 8192 so final JSON survives high-reasoning runs.
- 2026-05-05: Raised the AI route input payload budget to 4MB and documented it as app-side support for DeepSeek-V4-Pro's 384K-token-scale Think Max context recommendation, with a focused regression test proving multi-megabyte valuation context passes the route gate.
- 2026-05-05: Reworked the AI scenario-analysis browser payload into an explicit valuation context packet containing company identity/source links, current assumptions, scenario values/range, KPIs, statements, projections, sensitivity, Monte Carlo, provenance, replay metadata, and model instructions; added a focused regression test for the context contract.
- 2026-05-05: Added server-side Convex enrichment for AI scenario analysis: the route now queries allowlisted imported facts/artifacts and recent valuation run summaries, describes that Convex data contract in the prompt, and merges it into `convexContext` before calling Hugging Face.
- 2026-05-05: Expanded AI Convex enrichment into a fuller valuation research packet: company cache, cached statement history, approved imports/artifacts, recent run trend, latest successful run detail with trace, and reference-data catalog summary, all allowlisted server-side before prompting the model.
- 2026-05-05: Replaced the thin AI scenario-analysis system message with a detailed valuation analyst prompt covering evidence hierarchy, Convex context contract, scenario construction rules, output schema, fact-precedence, anti-invention, and no-chain-of-thought requirements.
- 2026-05-05: Live-tested AI analysis with the enriched valuation/Convex prompt. DeepSeek-V4-Pro max reasoning can return null final content or time out, so the route now retries once without `reasoning_effort`; the live retry path returned valid context-aware base/bull/bear JSON in about 158 seconds.
- 2026-05-05: Set the Hugging Face router provider preference as part of `HUGGING_FACE_MODEL` (`deepseek-ai/DeepSeek-V4-Pro:fastest`) rather than the AI response schema, updated docs/env defaults, and revalidated the AI route/context tests, lint, and production build.
- 2026-05-05: Fixed the dashboard refresh/search loading trap by consuming `?company-search=...` once, removing it from the URL, and adding a valuation-request timeout so failed EDGAR/backend calls render an error instead of an endless skeleton. Verified with focused compute/dashboard/AI tests, lint, build, and a local browser smoke showing one request per endpoint.
- 2026-05-05: Fixed the local dashboard error stack by allowing the explicit localhost rate-limit bypass to cover `0.0.0.0` dev-server URLs, restoring local EDGAR/engine env settings, and restarting `localhost:3000`. Verified settings/search/facts/DCF preview all returned `200`, then reran focused rate-limit/compute tests, lint, build, and a browser smoke for AAPL.
- 2026-05-05: Fixed AI analysis no-op behavior: DeepSeek could return the exact current assumptions while still succeeding, making Apply AI Analysis appear broken. The route now detects cached/provider no-op outputs, retries once with an explicit distinct-assumptions instruction, and avoids serving no-op cache entries. Verified with route regression tests, lint, build, and a browser apply-flow check with mocked AI output.
- 2026-05-05: Hardened the AI prompt packet after repeated same-default outputs across tickers: `currentAssumptions` still travels from browser to server for no-op validation, but is stripped from the model evidence packet so dashboard defaults cannot be mistaken for company fundamentals. Retry prompts include the exact current values only as forbidden values, not evidence. Verified focused AI tests, lint, build, and browser apply-flow smoke.
- 2026-05-05: Live-tested Apply AI Analysis and found two more provider failure modes: provider 5xx/timeouts and invalid response shape, plus a visible no-change case where only inactive scenarios changed. The AI route now retries transient provider errors, invalid-shape responses, full no-op outputs, and unchanged active-scenario outputs; parser also accepts `{ analysis: { base, bull, bear } }` wrappers. Verified focused AI tests, lint, build, and restarted localhost.
- 2026-05-05: Stress-tested Apply AI Analysis against live Hugging Face DeepSeek-V4-Pro. Found max-reasoning responses can spend the output budget on hidden reasoning (`finishReason: length`, `content: null`) and that terminal-growth-only changes made the UI look applied while preserving default drivers. Added provider attempt timeouts, a concise no-reasoning retry prompt, compact JSON fallback with enough output budget, sanitized provider diagnostics, and a material-change guard that rejects terminal-only active-scenario tweaks. Verified focused AI/context tests, lint, build, mocked browser apply-flow, and live AAPL/MSFT browser runs.
- 2026-05-05: Revalidated Apply AI Analysis through the in-app Browser surface. Found the engine recomputed and the right-panel sliders updated, but the hero assumption chips still showed static scenario presets. Wired `ValueCard` chips to the active assumptions and verified in Browser that AAPL AI output updates hero Growth/Margin/WACC consistently with the sliders.
- 2026-05-05: Fixed browser review comments on the valuation hero: the "What Drives the Range" list now derives from active assumptions/scenario instead of a static fallback, the fair-value marker stops at the empirical distribution curve instead of extending above it, and Bull/Bear markers clamp to the histogram edge when fair value sits outside the sampled range. Verified with focused component tests, lint, and `npm run build`.
- 2026-05-05: Clarified and fixed out-of-range fair-value markers on the distribution chart: when the scenario fair value sits outside the empirical Monte Carlo range, the chart now shows an explicit edge marker labelled `FV > range`/`FV < range` instead of an invisible near-baseline line or a misleading full-height line. Verified with focused chart tests, lint, and `npm run build`.
- 2026-05-05: Raised Monte Carlo simulation presets for stronger statistical stability: `fast` now runs 5,000 simulations, live `default` runs 25,000, and `high` runs 100,000; import approval now computes with the 25,000-run default profile. Updated tests/docs and verified focused Monte Carlo/DCF/chart tests, lint, and `npm run build`.
- 2026-05-05: Fixed the interface break after raising Monte Carlo runs: the web preset sent 25,000 simulations but the Python engine schema still capped `monteCarlo.runs` at 20,000, causing DCF preview failures. Raised the engine limit to 100,000, added a schema regression test, restarted Next/FastAPI locally, and verified AAPL search plus a 25,000-run DCF preview response.
- 2026-05-05: Reworked Monte Carlo from static per-run assumptions to vectorized dynamic annual paths: revenue growth, margins, WACC, tax, and sales-to-capital now evolve over forecast years with mean reversion and optional one-factor correlation, while terminal growth/spread remain sampled per simulation. The new NumPy kernel avoids per-sample Pydantic/trace overhead; local validation showed 25,000 runs through the web API in about 35ms. Verified Python Monte Carlo tests, JS preset/chart tests, lint, and `npm run build`.
- 2026-05-05: Clarified Monte Carlo distribution semantics in the hero card: the percentile band is now labeled `MC P10-P90`, the chart is captioned `Histogram min/max`, and the card exposes a recomputing state while scenario output refreshes. Cleared a stale Next dev `.next` cache after Browser Use hit a missing chunk overlay, restarted the dev server, and visually verified Base/Bull labels and out-of-range markers in Browser Use.
- 2026-05-05: Live-tested Apply AI Analysis from Browser Use while watching Next/FastAPI logs. Found DeepSeek-V4-Pro provider timeouts were retried as transient errors, holding the UI in `Analyzing...` for roughly 334 seconds; split local provider timeouts into a non-retryable error so the UI returns after one timeout window, and added a route regression test. Verified focused AI/UI tests, lint, and Browser Use timeout behavior.
- 2026-05-05: Tested GLM 5.1 for AI scenario analysis by running Next locally with `HUGGING_FACE_MODEL=zai-org/GLM-5.1`. HF Router rejected the DeepSeek-shaped request with `400 Input validation error`, so the AI route now retries provider input-validation failures once with a simpler OpenAI-compatible request while preserving server-side strict scenario validation. Browser Use verified GLM 5.1 returned a live AAPL analysis in about 43 seconds and triggered EDGAR facts plus DCF recompute successfully.
- 2026-05-05: Audited the AI valuation context and GLM-generated assumptions. Confirmed the browser payload carries company identity, valuation outputs, KPIs, statements, projections, sensitivity, Monte Carlo, provenance, and Convex import/history state, while the server strips `currentAssumptions` from model evidence and keeps them only for no-op detection. Tightened prompt wording so retry-only forbidden dashboard values cannot be cited as KPI/history/filing/projection evidence, then verified with focused tests and a Browser Use apply run.
- 2026-05-06: Fixed sensitivity heatmap correctness: the Python engine now emits rows as WACC offsets and columns as growth offsets, sensitivity/KPI context follows the active scenario, the frontend payload forwards `scenario`, and the UI renders a 9x9 +/-4pp heatmap with actual rate labels. Verified API and browser behavior for Base/Bull/Bear plus slider-driven recompute.
- 2026-05-06: Fixed EDGAR latest-filing selection for valuation facts. AAPL had fallen back to stale 2018 revenue because the extractor preferred the old `Revenues` tag and accepted quarterly-duration facts marked `fp=FY`; the extractor now prefers the modern revenue tag, merges older fallback tags, and filters short-duration quarterly entries. Verified live AAPL valuation uses FY 2025 period end `2025-09-27` filed `2025-10-31`.
- 2026-05-06: Added valuation freshness guardrails across the web path: SEC listings always prefer live EDGAR facts over imported Convex facts, company facts and DCF compute responses are `no-store`, workbench payloads always use the newest FY statement and fail if that latest statement lacks required revenue/shares, and provenance shares the same latest-statement selector. Verified live AAPL/MSFT/NVDA valuations use their newest FY periods.
- 2026-05-06: Fixed the docked assumptions sidebar scroll regression by locking the app shell to the viewport, keeping scroll inside the central workspace, and compacting the right-panel controls at shorter desktop heights. Verified at the reported `1710x790` viewport that the document has no global scroll and the right sidebar remains fixed.
- 2026-05-06: Configured local Convex for AI/context flows by creating a local `dcf-dashboard` Convex deployment, wiring `.env.local` with server/client Convex URLs plus a local sync token, setting the same token in Convex env, and running Next/Convex in detached `screen` sessions. Verified `/api/settings/status` reports Convex/history/import readiness.
- 2026-05-06: Live-tested AI analysis with Convex enabled and fixed the uncovered daily-cap blocker: the AI route used a 24-hour public cap, but Convex `securityRateLimit` only accepted one-hour windows, returning `RATE_LIMIT_UNAVAILABLE` before the provider call. Raised the Convex cap horizon to 24 hours, added regression coverage, and verified a live AAPL DeepSeek run updates assumptions and recomputes DCF.
- 2026-05-06: Moved AI scenario rationales out of the clipped assumptions sidebar into a workspace panel below the valuation hero, with a live progress stream shown while the model is running. Verified in Browser Use that the sidebar remains usable, the stream appears under the hero, and final rationales render in the main workspace.
- 2026-05-06: Fixed the AI analysis invalid-shape fallback after the live DeepSeek run still failed in the browser: the route now escalates repeated invalid JSON shapes to the compact strict prompt instead of spending a third normal retry. Verified focused AI/UI tests, lint, production build, restarted localhost, and Browser Use confirmed a live AAPL run shows the stream, applies assumptions, recomputes valuation, and renders scenario rationales.
- 2026-05-06: Added an AI analysis progress bar above the model stream so long Hugging Face waits have a visible loading indicator until the final rationale arrives. Verified component/route tests, lint, production build, Browser Use progress-bar visibility, and restored the local FastAPI SEC user-agent so AAPL valuation loads from EDGAR again.
- 2026-05-06: Switched the local AI scenario model from DeepSeek-V4-Pro to `zai-org/GLM-5.1` after DeepSeek timed out in the browser. Verified settings reports GLM 5.1, Browser Use shows the loading progress bar, and a live AAPL GLM run completes successfully with applied assumptions plus DCF recompute.
- 2026-05-06: Adjusted the AI loading bar to start at a true 0% instead of 29% by treating the first streamed line as setup rather than completed progress and removing the fill's minimum width. Verified unit coverage, lint, build, and Browser Use showing `aria-valuenow=0` with a visible `0%` label.
- 2026-05-06: Grounded AI scenario rationales in actual EDGAR statement history by extracting operating income/margin, carrying those fields through the workbench/Convex/UI/AI context, deriving statement-trend metrics, and rejecting AI rationales that cite unsupported business or market topics. Verified focused Bun/Python tests, Convex typecheck, lint, live AAPL EDGAR facts/DCF preview, and a live GLM 5.1 AI analysis.
- 2026-05-09: Added server-side AI input token accounting for the scenario-analysis route. The route now counts the exact chat payload sent to Hugging Face with a local tokenizer estimate, upgrades to provider `usage.prompt_tokens` when available, caches token usage with AI results, and surfaces input tokens/payload size/tokenizer source in the workspace AI panel without exposing prompt text. Verified focused AI route/UI tests, lint, and Convex typecheck.
- 2026-05-09: Implemented the visual polish fix plan: added missing spacing/on-accent tokens, removed hardcoded accent leaks, added a P10-P90 band to the Monte Carlo distribution, cleaned the misleading display/font globals, hid the redundant desktop search submit icon, and replaced right-panel unicode driver arrows with Radix icons. Verified focused component tests, lint, and Convex typecheck; production build remains blocked by the existing local Next SWC code-signing issue.
- 2026-05-10: Fixed the local dashboard boot path after Browser Use showed the app was not functioning: rate-limit now falls back to in-memory buckets in local development when configured Convex is unavailable, while production remains fail-closed. Restarted Next with a local SWC WASM fallback and FastAPI with local EDGAR/unsigned-dev env, then verified settings, AAPL search/facts, DCF preview, and the Browser-rendered fair value dashboard.
- 2026-05-11: Addressed stacked PR review feedback by preserving reviewed import number formats, requiring internal auth for import context, mounting the import workspace for import-required search selections, and replaying saved scenario assumptions in the hero card. Verified focused Bun coverage and production build on the rebased stack.
- 2026-05-11: Remediated the browser valuation replay info-disclosure finding by keeping browser replay responses on an explicit safe-field allowlist and adding regression coverage that trace-derived projections, KPI/history values, assumptions, sensitivity matrices, and Monte Carlo summaries stay redacted.
- 2026-05-11: Remediated the Aardvark-reported Damodaran Excel parser memory DoS by replacing eager worksheet row materialization with row-by-row iteration and adding parser regression coverage. Focused sync tests pass; broader security-focused test remains blocked by missing local `httpx`.
